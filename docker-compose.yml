services:
  valkey:
    image: valkey/valkey:alpine
    container_name: real-time-chatapp
    ports:
      - "6379:6379"
    volumes:
      - valkey-data:/data
    restart: unless-stopped

volumes:
  valkey-data: